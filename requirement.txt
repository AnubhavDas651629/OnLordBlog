greenlet (for async)
aiosqlite

for authentication ->
"pwdlib[argon2]"
pyjwt
pydantic-settings

for photo related tasks ->
pillow

for token and password chaning install the lib
aiosmtplib

for postgress method ->
1)firstly install -> brew install postgresql@18
2)then run postgresql -> brew services start postgresql@18
3)to create a database user with a password ->  psql postgres -c "CREATE USER bloguser WITH PASSWORD 'blogpass';"
4)now create a database and amking the user the owner of the databse -> createdb blog -O bloguser
5) now verify if everything is wokring properly -> psql blog -U bloguser 
\dt for finding existing tables
\q for exiting out of blog

uv add "psycopg[binary]" -> for running it locally via python


for alembic installation ->
1)uv add alembic
2)uv run alembic init -t async alembic  (the -t async tells to load the whole async manual and the last alembic is for telling where our migration files will go which is hte alembic/versions)
after doing this move on to the alembic.ini file and scroll down to sqlalchemy.url and delete the url given there, as we are manaully doing it 
then move on to alembic/env.py -> import models, from config import settings , from database import Base
look for #-----------ADDED----------------

alembic migrations ->
uv run alembic revision --autogenerate -m "initial schema"
uv run alembic upgrade head -> for up to dating the amebinc schemas
uv run alembic current -> for current status
alembic downgrade -1 -> to downgrade a step



boto3 - official aws sdk for python

pytest - for testing

uv add --dev pytest 
uv add --dev "moto[s3]"

uv run pytest tests/test_demo.py -v (to run the tests)
uv run pytest tests/ -s -> if we want to see the outputs of the tests

postgres testing method used-
createdb test_blog -O bloguser

